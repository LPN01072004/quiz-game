{
  "name": "my-package",
  "version": "1.0.0",
  "description": "A project setup with Babel and Jest for successful deployment",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib", // Specify source directory
    "prebuild": "jest --passWithNoTests",
    "test": "jest",
    "start": "node lib/index.js"
  },
  "engines": {
    "node": ">=16",
    "yarn": ">=1.22"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@babel/core": "^7.22.10",
    "@babel/preset-env": "^7.22.10"
  },
  "devDependencies": {
    "@babel/cli": "^7.22.10",
    "jest": "^29.6.2"
  },
  "babel": {
    "presets": ["@babel/preset-env"]
  }
}
