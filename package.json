{
  "name": "my-package",
  "version": "1.0.0",
  "description": "A sample project demonstrating yarn scripts and Babel setup",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src -d lib",
    "prebuild": "jest",
    "test": "jest",
    "env": "yarn run env"
  },
  "engines": {
    "node": ">=16",
    "yarn": ">=1.22"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@babel/core": "^7.22.10",
    "@babel/preset-env": "^7.22.10"
  },
  "devDependencies": {
    "@babel/cli": "^7.22.10",
    "jest": "^29.6.2"
  },
  "babel": {
    "presets": ["@babel/preset-env"]
  }
}
